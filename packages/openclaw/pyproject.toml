[project]
name = "openclaw-corpus"
version = "0.1.0"
description = "Corpus Protocol skill for OpenClaw — turn your agent into a revenue-generating Corpus"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
